#ifndef LABA_FUNCTION_H
#define LABA_FUNCTION_H

int Operator (char);

int find (int*, int, int);

int chist (int*, int);

int del (int*, int*, int);

int umn (int*, int*, int);

int plus (int*, int*, int);

int minus (int*, int*, int);

int deistrev (int, int, int);

int Check (int*, int);

int Checkrev (int*, int);

#endif //LABA_FUNCTION_H